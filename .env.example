# DMG AI Flight Simulator — Frontend environment variables
# Copy to .env and adjust before running

# Set to 'false' to route through the real Demo Gateway instead of mock data
# Requires the gateway to be running (cd gateway && npm run dev)
VITE_USE_MOCK=true
